{% extends 'base.html.twig' %}

{% block title %}
    {% if edit %}
        Edit Session
    {% else %}
        Add Session
    {% endif %}
{% endblock %}

{% block body %}

    <div class="breadcrumb">
        <p class="row">
            <a href="{{ path('app_home')}}">Dashboard</a>&nbsp;>&nbsp;<a href="{{ path('app_session')}}">Sessions</a>&nbsp;>&nbsp;<span class="blue">Add Session</span>
        </p>
    </div>

    <div class="content-container">

        {% if edit %}
            <h1>Edit Session</h1>
            
        {% else %}
            <h1>Add Session</h1>
        {% endif %}
    
        {{ form_start(formNewSession) }}
    
            {{ form_row(formNewSession.name) }}
        
            {{ form_row(formNewSession.place) }}
        
            {{ form_row(formNewSession.dateStart) }}
        
            {{ form_row(formNewSession.dateEnd) }}
        
            {{ form_row(formNewSession.teacher) }}
        
            {{ form_row(formNewSession.formation) }}

            {{ form_row(formNewSession.submit) }}
    
        {{ form_end(formNewSession, {'render_rest': false}) }}
        
    </div>

{% endblock %}